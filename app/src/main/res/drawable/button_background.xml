<!-- res/drawable/button_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#000000"/> <!-- Button background color (black) -->
    <stroke
        android:width="1dp"
        android:color="#858383"/> <!-- Button border color (white) -->
    <corners android:radius="50dp"/> <!-- Optional: Corner radius for rounded corners -->
    <padding
        android:left="6dp"
        android:top="16dp"
        android:right="6dp"
        android:bottom="16dp"/> <!-- Optional: Padding inside the button -->
</shape>
